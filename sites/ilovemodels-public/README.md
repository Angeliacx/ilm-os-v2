# I Love Models public site

Public showcase intended for `ilovemodels.fr`.

Target architecture:

- `ilovemodels.fr` public model-facing website
- `os.ilovemodels.fr` private ILM Agency OS dashboard
- model domains for clean deeplinks

Before connecting the final domain, update the application contact destination if `contact@ilovemodels.fr` is not active yet.
